name: C/C++ CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

# comments
    steps:
    - uses: actions/checkout@v4
#    - name: configure
#      run: ./configure
    - name: makefile on repository root directory
      run: make all
#    - name: make check
#      run: make check
#    - name: make distcheck
#      run: make distcheck
